# IP 화이트리스트 설정
# 아래 목록에 포함된 IP/대역만 접근 허용
# DEV_MODE=true일 때는 이 설정이 무시됨

allowed_ips:
  - "127.0.0.1"          # localhost
  - "::1"                # localhost (IPv6)
  - "172.16.0.0/12"      # Docker 내부 네트워크
  - "192.168.0.0/16"     # 사내 네트워크
  # 필요시 특정 IP 추가:
  # - "10.0.1.50"
